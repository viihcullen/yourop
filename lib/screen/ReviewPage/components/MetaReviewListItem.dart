import 'package:flutter/material.dart';
import 'package:yourop/models/content.dart';

class MetaReviewListItem extends StatelessWidget {
  final MetaReview metaReview;
  const MetaReviewListItem({super.key, required this.metaReview});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}